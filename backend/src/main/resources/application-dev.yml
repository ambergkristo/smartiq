spring:
  datasource:
    url: jdbc:h2:mem:smartiq_dev;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""

smartiq:
  import:
    fail-on-category-threshold: ${STRICT_DATASET_VALIDATION:${SMARTIQ_IMPORT_FAIL_ON_CATEGORY_THRESHOLD:true}}
  bank:
    trigger-pipeline-on-low-bank: false
    block-on-low-bank: false
